import { ButtonList, VideoContainer } from "../components";

function MainContainer() {
    return (
        <div>
            <ButtonList />
            <VideoContainer />
        </div>
    );
}

export default MainContainer;